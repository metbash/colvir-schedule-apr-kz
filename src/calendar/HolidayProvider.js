/**
 * ==========================================================
 * Colvir Schedule & APR Calculator (KZ)
 * Version: 4.0-dev1
 *
 * HolidayProvider.js
 *
 * Базовый поставщик праздничных дней.
 * ==========================================================
 */

export default class HolidayProvider {

    /**
     * Возвращает true,
     * если дата является праздничной.
     *
     * @param {Date} date
     * @returns {boolean}
     */
    isHoliday(date) {

        return false;

    }

}